name: Daily Job Data Sync Pipeline

on:
  schedule:
    # Triggers every day at 12:00 AM UTC (5:30 AM IST)
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository Code
      uses: actions/checkout@v3

    - name: Set up Python Environment
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install requests psycopg2-binary streamlit

    - name: Setup Streamlit Secrets Dynamically
      env:
        STREAMLIT_SECRETS_DATA: ${{ secrets.STREAMLIT_SECRETS }}
      run: |
        mkdir -p .streamlit
        echo "$STREAMLIT_SECRETS_DATA" > .streamlit/secrets.toml

    - name: Run Data Cleaning and Preprocessing Pipeline
      run: python pipeline.py
